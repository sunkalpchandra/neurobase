/** Shared class fragments from docs/DESIGN.md so every component spells them the same way. */

/** Micro label: column headers, type labels, eyebrows. */
export const microLabelClass = "text-2xs font-medium uppercase tracking-wide text-ink-muted";

/** Bordered surface used by cards, tables and panels. */
export const panelClass = "rounded-lg border border-line bg-surface";

/** Page-width container from the layout rules. */
export const containerClass = "mx-auto w-full max-w-[1440px] px-4 md:px-6";

/** Text link inside body copy. */
export const linkClass =
  "text-accent underline decoration-accent-line underline-offset-2 hover:decoration-accent";
